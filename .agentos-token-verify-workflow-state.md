# #2 content-backend token 校验 SDD — workflow-state

> 紧凑档 A 类（presence 强制）。跨会话恢复先读本文件 + plan + git log。

## 任务

- **任务**：IAM 前置 #2 — content-backend 收 envelope v2 user_token → JWT 自验(JWKS) → ops /api/internal/bindings lookup 验 token 绑设备（IAM §6.3 step3+4）
- **类型**：中任务（SDD）
- **repo**：`~/projects/agentos-content-backend`（独立 sibling）
- **worktree**：`~/projects/agentos-content-backend-token-verify`，branch `token-verify-sdd`，base `origin/main e681e17`
- **主模型**：glm-5.2[1m]（固定，不切）；worker Claude sonnet/opus subagent + codex gpt-5.5 跨厂商
- **档位**：紧凑（命中信任边界/JWT/终端用户身份强制子集）
- **跨会话冲突**：content-backend repo 无其他 worktree；窗口C 在 AgentOS repo 做 3f（独立 repo 无冲突）；窗口C 3f-ab-asr-tts-spec worktree 在 AgentOS repo（范围不重叠）

## skill 链（老林 2026-07-09 确认）

1. ✅ superpowers:brainstorming — spec `docs/superpowers/specs/2026-07-09-content-backend-token-verify-design.md` committed
2. ✅ superpowers:writing-plans — plan `docs/superpowers/plans/2026-07-09-content-backend-token-verify.md` committed（7 task TDD）
3. ✅ plan-*-review fold — Eng+DevEx+Codex 跨厂商三路全 NEEDS_REVISION→fold 15 findings（12P1+3P2）全落地；spec 加 D7(preHandler 顺序 sim 偏离)+D8(错误响应 wrapEnvelope)；GATE PASS
4. ⬜ SDD（subagent-driven）+ per-task review + final whole-branch opus review + docker e2e — **next**
5. ⬜ verification-before-completion + Verifier + codex 跨厂商 diff review
6. ⬜ 用户确认后 gstack /ship

## 范围边界

- **归本任务**：envelope v2 解析 + JWT 自验(step3) + ops lookup 绑定校验(step4) 真实；401/403/503 失败语义；audit `^end_user:<id>`；env 6 字段；vitest unit + 三 service docker-compose e2e
- **stub defer**：region(step5)/entitlement(step6)/mTLS caller(step2) — capability_mode=mock 诚实声明
- **defer 别窗口**：mTLS caller(#6 窗口A) / device-hub forwarding(#5 窗口A) / region 真校验(独立子项) / entitlement 真系统(#7 §5.2) / lookup 缓存 / IAM 多 aud(D1)

## 依赖状态

- envelope v2 schema ✅ PR#61 `78c8274`
- ops /api/internal/bindings lookup + service-auth ✅ #4 PR#15 `764d6ce`
- IAM JWKS + token 签发 ✅ sibling 1.0+1.1 `7ccc79f`
- content-backend 侧 ❌ 待 SDD

## not-architecture-impact

消费 envelope v2 既有字段，不改 schema/契约/子系统。PR 描述附 negative-claim 证据（touched files + 不命中理由），reviewer 显式核查。

## ledger（progress）

- spec commit：`docs/superpowers/specs/2026-07-09-content-backend-token-verify-design.md`（242 行，8 节，6 决策 D1-D6）
- plan commit：`docs/superpowers/plans/2026-07-09-content-backend-token-verify.md`（1314 行，7 task TDD bite-sized + self-review）
- 当前阶段：plan-*-review fold（链第 3 项）
